# /desk — full communications review & reply cycle

Run the complete on-demand review across all channels. Same legs as the daily routine,
run now: slack_sweep + unanswered (read ALL of it) + DM checklist via the user-scoped
connector; Gmail AND Outlook (one combined reply queue, source-tagged); Otter since the
last run via /meeting; incoming originals filed with links; every to-do into the Tasks
DB; idea-class items through /idea-intake with counts.

Output: the digest — top actions, overdue by name, every reply DRAFTED with the
original side by side awaiting approval (rule 1: never auto-reply, never a question
after a draft), files filed, new tasks and ideas with counts (loud zero, rule 5).
Every claim carries its source per rule 9; unverified items are marked, never hidden.
